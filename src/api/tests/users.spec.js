import {
  expect,
} from 'chai';

const { describe, it } = global;

describe('API.Users', () => {
  describe('#create()', () => {
    it('should be ok', () => {
      expect(true).to.equal(true);
    });
  });
});

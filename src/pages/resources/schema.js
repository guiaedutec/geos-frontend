import { FormattedMessage } from "react-intl";
import React from "react";

export default {
  email: {
    presence: {
      message: <FormattedMessage id="SchemaResources.email.presence.message" />,
    },
    email: {
      message: <FormattedMessage id="SchemaResources.email.message" />,
    },
  },
};
